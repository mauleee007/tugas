import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF37404a);
const kTextColor = Color(0xFF546ca4);
const kIconColor = Color(0xFF99a5b1);

const kDefaultPadding = 20.0;

final kDefaultShadow = BoxShadow(
  offset: Offset(5, 5),
  blurRadius: 10,
  color: kTextColor.withOpacity(0.56),
);
